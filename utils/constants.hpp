#ifndef CONSTANTS
#define CONSTANTS

static const int TILE_COUNT = 40000;

static const int WIN_WIDTH = 600;
static const int WIN_HEIGHT = 600;

static const float NEAR = 0.1f;
static const float FAR = 200.f;

// projectiles
static const float P_LIFETIME = 20.f; // 20 seconds
static const float P_SPEED = 100.f; // units per second

#endif // !CONSTANTS
